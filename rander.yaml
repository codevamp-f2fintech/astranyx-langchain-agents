services:
  - type: worker
    name: hastranyx-langchain-agents
    runtime: docker 
    dockerfilePath: ./Dockerfile


    buildCommand: |
      pip install -r requirements.txt
      bash build.sh

    startCommand: python main.py